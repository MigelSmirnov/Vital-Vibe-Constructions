version: 1

phase: knowledge-foundation

active_branch: agent/architecture-sandbox

current_target:
  - continue next content-model entity when source records are ready
  - decide whether browser screenshot tooling is needed
  - defer legacy structural audit warnings unless explicitly targeted

legacy:
  runtime: x-dc
  editable: false
  migration_strategy: strangler-pattern

next_runtime:
  root: site-next
  status: active-development

completed:
  - architecture contracts
  - engineering process
  - content extractor
  - crawlability audit
  - ai-native foundation
  - initial site-next
  - knowledge layer
  - EntityCore
  - Service
  - Knowledge Repository
  - JSON-compatible content table adapter
  - site-next build connected to knowledge repository
  - repository uniqueness validation for service ids and slugs
  - ExternalApp knowledge entity
  - repository uniqueness validation for external app ids
  - Site knowledge entity
  - content table parser strategy decision
  - homepage media inventory
  - measured homepage raster media dimensions
  - Media knowledge entity
  - media table connected to Knowledge Repository
  - media uniqueness validation for ids, slugs, and src values
  - Project knowledge entity
  - project content table
  - projects connected to Knowledge Repository
  - project uniqueness validation for ids and slugs
  - repository cross-reference validation for project/media/service references
  - site-next project section projected from Knowledge Repository
  - focused node:test coverage for Project and Knowledge Repository invariants
  - local check runner for knowledge tests, site-next build, content audit, and whitespace checks
  - static site-next project projection review
  - project section summary and equal-height project card layout refinements
  - legacy index head metadata cleanup
  - Organization JSON-LD added to legacy index
  - legacy content audit reduced to 0 errors
  - ContactDetails knowledge entity
  - contact details content table
  - contact details connected to Knowledge Repository
  - focused contact channel validation tests
  - site-next contact section projected from ContactDetails
  - site-next Organization JSON-LD contactPoint projected from ContactDetails

blocked:
  - legacy runtime cannot tolerate structural HTML changes
  - browser screenshot review tooling is not installed in this repository

content_tables:
  current_format: json-compatible-yaml
  parser: JSON.parse
  yaml_parser_policy: add only when yaml-specific authoring features are required
  media_records: homepage-visible raster images only
  project_records: homepage-visible projects only
  contact_details_records: public contact channels only

checks:
  command: node tools/checks/run.mjs
  latest_result: pass
  content_audit_findings: 0 errors and 5 warnings in legacy content audit
  remaining_legacy_warnings:
    - heading-level-skip
    - missing-main
    - heavy-inline-styles
    - runtime-dependent-root
    - large-inline-script
  static_site_next_review:
    metadata: pass
    landmarks: pass
    project_cards: 3
    local_assets: pass
    duplicate_ids: pass
    image_dimensions: pass

next_update_required: true

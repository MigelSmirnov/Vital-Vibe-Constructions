version: 13

updated_at: 2026-07-29T00:00:00Z
phase: root-domain-publication-preparation
stage_status: ready
active_branch: agent/architecture-sandbox
pull_request:
  number: 1
  state: ready-for-review
  base: main
  head: agent/architecture-sandbox

contract_references:
  architecture: architecture/app-architecture.yaml
  invariants: architecture/invariants.yaml
  constants: architecture/constants.yaml
  routes: architecture/project-routes.yaml
  gallery_coverage: architecture/gallery-media-coverage.yaml

latest_verified_baseline:
  commit: 92696090524cb6e12607c6a14334deb7f6b47349
  summary: Root public paths and all generated projections validated locally from this base commit plus the current task changes.
  checks:
    workflow: local full project checks
    conclusion: success
    knowledge_tests:
      passed: 22
      failed: 0
    gallery_media_coverage:
      modeled: 39
      required: 39
      unmodeled: 0
    generated_html_pages: 7
    root_path_validation: pass
    project_detail_pages: 4
    gallery_pages: 1
    sitemap_urls: 7
    content_audit:
      errors: 0
      warnings: 5
    diff_whitespace: pass
  command: node tools/checks/run.mjs
  environment: local

current_strategy:
  legacy_runtime: read-only
  next_runtime: site-next
  source_of_truth: Knowledge Repository
  gallery_coverage: complete-legacy-coverage
  public_path_mode: root-relative
  public_path_rules:
    stylesheet: /styles.css
    logo: /VVC_primary_logo.svg
    project_routes: /projects/{project-slug}/
    contact: /#contacto
  generated_projections:
    - site-next
    - llms.txt
    - sitemap.xml

route_state:
  projects_index:
    path: /projects/
    status: generated
    sitemap_eligible: true
  project_detail:
    pattern: /projects/{project-slug}/
    status: generated
    instance_count: 4
    sitemap_eligible: true
  gallery_index:
    path: /gallery/
    status: generated
    sitemap_eligible: true
  project_image_detail:
    pattern: /projects/{project-slug}/images/{media-slug}/
    status: deferred
    sitemap_eligible: false

media_migration:
  total_content_media: 39
  project_owned_media: 37
  modeled_non_project_media: 2
  unmodeled_media: 0
  target_unmodeled_media: 0
  coverage_gate: passed

active_stage:
  goal: Prepare site-next for publication at the domain root without changing the legacy runtime.
  status: completed
  scope:
    - normalize local generated HTML href and src values to root-relative public URLs
    - preserve same-page homepage anchors
    - preserve canonical, Open Graph, HTTPS, mailto, tel, and WhatsApp URLs
    - validate root-relative URLs before page-specific generated output validators
    - keep generated site-next, llms.txt, and sitemap.xml reproducible
  exclusions:
    - do not edit support.js or legacy entrypoints
    - do not change the legacy public runtime
    - do not weaken or remove existing validators
  next_verification:
    - node tools/checks/run.mjs
    - git diff --check
    - verify deployed root-relative assets return HTTP 200
    - inspect site-next at desktop and mobile sizes after deployment

next_stage:
  goal: Verify the generated site in its root-domain hosting environment before switching public traffic.
  implementation_steps:
    - confirm the host publishes site-next as the domain document root
    - verify CSS, logo, project media, project routes, and contact anchors over HTTP
    - perform desktop and mobile visual inspection
    - preserve the legacy runtime until the replacement deployment is validated

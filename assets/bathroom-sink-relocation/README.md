# Bathroom sink relocation photographs

Five owner-supplied work-stage JPEGs, copied byte-for-byte and renamed for stable website paths. Their original filenames, dimensions, SHA-256 checksums and suggested sequence are in `sandbox/articles/media/bathroom-sink-relocation/index.json`.

The five new Media records belong to the existing `bathrooms` service until the case receives a validated Project record. The final homepage image is reused under its existing `media-home-bathroom` ID and existing path; it was previously AI-retouched, not retouched for this package.

No project location, technical compliance, test results, publication date or materials cost is inferred from the photos. This media import does not publish an article or create a route.
